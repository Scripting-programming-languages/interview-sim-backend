# interview-agent-backend

## Локальный запуск

Для локального запуска проекта выполните команду `docker compose up -d`

Чтобы подключиться к базе данных:
* url: jdbc:postgresql://localhost:5432/mydatabase
* username: myuser
* password: secret

URL для подключения к kafka-ui: http://localhost:8090/